import { IGet, IRequestOptions, IPolygonQuery } from "../transport/request.js";
import { IAggsResults } from "./aggregates.js";
export interface IAggsPreviousCloseQuery extends IPolygonQuery {
    adjusted?: "true" | "false";
}
export interface IAggsPreviousClose {
    ticker?: string;
    adjusted?: boolean;
    queryCount?: number;
    request_id?: string;
    resultsCount?: number;
    status?: string;
    count?: number;
    results?: IAggsResults[];
}
export declare const previousClose: (get: IGet, symbol: string, query?: IAggsPreviousCloseQuery, options?: IRequestOptions) => Promise<IAggsPreviousClose>;
