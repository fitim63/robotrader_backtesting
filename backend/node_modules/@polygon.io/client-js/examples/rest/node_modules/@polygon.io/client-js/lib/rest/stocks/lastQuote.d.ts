import { IGet, IPolygonQuery, IRequestOptions } from "../transport/request.js";
import { ILastTradeInfo } from "./lastTrade";
export interface ILastQuote {
    request_id?: string;
    status?: string;
    results?: ILastTradeInfo;
}
export declare const lastQuote: (get: IGet, symbol: string, query?: IPolygonQuery, options?: IRequestOptions) => Promise<ILastQuote>;
