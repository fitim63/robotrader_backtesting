import { IGet, IPolygonQuery, IRequestOptions } from "../transport/request.js";
import { ITick } from "./ITickJson";
export interface ICryptoDailyOpenCloseQuery extends IPolygonQuery {
    adjusted?: "true" | "false";
}
export interface ICryptoDailyOpenClose {
    close?: number;
    closeingTrades?: ITick;
    day?: string;
    isUTC?: boolean;
    open?: number;
    openTrades?: ITick;
    symbol?: string;
}
export declare const dailyOpenClose: (get: IGet, from: string, to: string, date: string, query?: ICryptoDailyOpenCloseQuery, options?: IRequestOptions) => Promise<ICryptoDailyOpenClose>;
