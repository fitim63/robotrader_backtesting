import { IGet, IPolygonQuery, IRequestOptions } from "../transport/request.js";
import { ILastTradeInfo } from "../stocks/lastTrade";
export interface IOptionsLastTrade {
    request_id?: string;
    status?: string;
    results?: ILastTradeInfo;
}
export declare const lastTrade: (get: IGet, symbol: string, query?: IPolygonQuery, options?: IRequestOptions) => Promise<IOptionsLastTrade>;
