import { IAggsQuery, IAggs } from "../stocks/aggregates.js";
import { IGet, IRequestOptions } from "../transport/request.js";
export declare const aggregates: (get: IGet, ticker: string, multiplier: number, timespan: string, from: string, to: string, query?: IAggsQuery, options?: IRequestOptions) => Promise<IAggs>;
